// join method, converts array entries into strings wtiht the addition that you can specify the separator to use

let animals = ['dog', 'cat', 'monkey', 'gerbil'];  

animals.join('!!');

console.log(animals);