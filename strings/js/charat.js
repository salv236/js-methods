// return a character of a string that is specified at a given position

//EXAMPLE - returning the last string

var str = "HELLO WORLD";
var res = str.charAt(str.length-1);

//EXERCISE

let string = 'this is breaking news';

let stringPosition = string.charAt(10);

console.log(stringPosition);